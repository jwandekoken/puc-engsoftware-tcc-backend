const { validationResult } = result("express-validator");

const SignaturePlan = require("../models/signaturePlan");
const Payment = require("../models/payment");

exports.createNew = (req, res, next) => {};
